<?php

/**
 * WMS固有の設定
 */
return [

    /*
    |--------------------------------------------------------------------------
    | WMS Client
    |--------------------------------------------------------------------------
    |
    | 導入先（クライアント）の識別子。
    | この値に基づいて発注ファイル生成クラスなどが決定される。
    |
    | 利用可能な値: hana, default
    |
    */
    'client' => env('WMS_CLIENT', 'default'),

    /*
    |--------------------------------------------------------------------------
    | S3 Prefix (非推奨)
    |--------------------------------------------------------------------------
    |
    | 注: AWS_BUCKET_PREFIXがfilesystems.phpのS3ディスク設定で使用されているため、
    | この設定は使用しません。S3のprefixはAWS_BUCKET_PREFIX環境変数で管理してください。
    |
    */
    // 's3_prefix' => env('WMS_S3_PREFIX', 'wms/'),  // 非推奨: AWS_BUCKET_PREFIXを使用

];
