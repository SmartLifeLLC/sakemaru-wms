/**
 * Loading overlay component
 * Controlled via Alpine reactive `isLoading` property in the app.
 * Usage: Include the loading overlay div in layout, bind to x-show="isLoading".
 */
export function loadingOverlayComponent() {
    return {
        // Component is purely template-driven.
    };
}
